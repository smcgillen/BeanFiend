<%= form_for @review do |f| %>
	<%= f.text_field :title %>
	<%= f.text_area :text %>
	<%= f.hidden_field :user, :value => current_user %>
	<%= f.hidden_field :store, :value => @store %>
	<%= f.submit "Post Review" %>
<% end %>
